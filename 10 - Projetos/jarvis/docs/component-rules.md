# Component Rules

- Somente componentes presentes em `packages/design-system/registry/components.ts` podem ser usados.
- Variantes devem estar listadas em `allowedVariants`.
- Componentes nao podem assumir usos em `forbiddenUses`.
- Cada secao deve mapear para composicao concreta antes de gerar JSX.
